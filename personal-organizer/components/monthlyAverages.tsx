import { SafeAreaView, View, TextInput, Text, ScrollView, Image } from 'react-native';

import { styles } from '@/constants/stylesheet';
import { streakData } from '@/constants/streaks';
import { formData, formKeysMinusDate, formKeysMinusDatePretty } from "@/constants/formData";

export default function monthlyAverages(){

    return(
        <View></View>
    )
}